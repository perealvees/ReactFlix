import styles from "./Footer.module.css";

function Footer () {
    return (
        <footer className={styles.footer}>
            <p>Página desenvolvida para aula de aprendizado de React</p>
        </footer>
    )
}

export default Footer;